# 复制为 .env 后按需填写（可合并原 .env.local 中的项）
# Docker Compose 会读取**本目录**的 .env 做变量替换，并通过 env_file 注入容器。

# 公开站点 URL（会打进前端 bundle，改后需 --build 重建镜像）
# NEXT_PUBLIC_SITE_URL=https://docs.example.com

# 供 compose 中端口映射，默认 3000
# PORT=3000
